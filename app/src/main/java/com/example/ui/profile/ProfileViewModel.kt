package com.example.ui.profile

import androidx.lifecycle.ViewModel
import com.example.data.repository.AuraRepository

class ProfileViewModel(private val repository: AuraRepository) : ViewModel() {
    // Add logic for profile/admin features if needed
}
